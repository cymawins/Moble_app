<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.Kiuda.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="@dimen/radius_large"
    app:cardElevation="@dimen/elevation_card_low"
    app:strokeColor="@color/kiuda_outline_variant"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Header: 아이콘 + 유형 태그 + 작물 이름 + 날짜 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <FrameLayout
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@drawable/bg_step_number_badge">

                <TextView
                    android:id="@+id/tvRecordIcon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:textSize="15sp"
                    tools:text="💧" />
            </FrameLayout>

            <TextView
                android:id="@+id/tvRecordCategory"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:textSize="11sp"
                android:textStyle="bold"
                android:textColor="@color/kiuda_primary"
                android:background="@drawable/bg_pill_soft"
                android:paddingHorizontal="8dp"
                android:paddingVertical="3dp"
                tools:text="관수/물주기" />

            <TextView
                android:id="@+id/tvPlantNameTag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:textSize="12sp"
                android:textColor="@color/kiuda_text_secondary"
                tools:text="초록이 · 몬스테라" />

            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <TextView
                android:id="@+id/tvDisplayDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="@color/kiuda_text_tertiary"
                tools:text="오늘 09:30" />
        </LinearLayout>

        <!-- Title -->
        <TextView
            android:id="@+id/tvRecordTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/kiuda_text_primary"
            android:lineSpacingExtra="2dp"
            android:includeFontPadding="false"
            tools:text="몬스테라 아침 정기 관수 완료" />

        <!-- Description Content -->
        <TextView
            android:id="@+id/tvRecordContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textSize="14sp"
            android:textColor="@color/kiuda_text_secondary"
            android:lineSpacingExtra="4dp"
            android:includeFontPadding="false"
            tools:text="토양 수분을 55% 수준으로 맞추고 잎의 먼지를 가볍게 닦아주었습니다." />

        <!-- Footer / Status Badge -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="10dp">

            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <TextView
                android:id="@+id/tvStatusBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="✓ 관리 완료"
                android:textSize="11sp"
                android:textStyle="bold"
                android:textColor="@color/kiuda_primary" />
        </LinearLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
