<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".KiudaApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobile_app">
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:theme="@style/Theme.Kiuda.Splash"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".presentation.onboarding.OnboardingActivity"
            android:screenOrientation="portrait"
            android:exported="false" />

        <activity
            android:name=".presentation.auth.login.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.auth.signup.SignupActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.dashboard.DashboardActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ask.AskActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ask.DiagnosisResultActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ncpms.EncyclopediaListActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ncpms.EncyclopediaDetailActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.ncpms.PestAlertsActivity"
            android:exported="false" />

    </application>

</manifest>